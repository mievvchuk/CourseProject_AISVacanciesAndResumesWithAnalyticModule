@model List<AisVacanciesAndResumes.ViewModels.Messages.MessageListItemViewModel>

@{
    ViewData["Title"] = "Надіслані повідомлення";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2 class="h4 mb-0">Надіслані повідомлення</h2>
    <div class="d-flex gap-2">
        <a asp-action="Inbox" class="btn btn-outline-secondary">Вхідні</a>
        <a asp-action="Create" class="btn btn-primary">Нове повідомлення</a>
    </div>
</div>

@if (!Model.Any())
{
    <div class="alert alert-info">Надісланих повідомлень немає.</div>
}
else
{
    <div class="card shadow-sm border-0">
        <div class="table-responsive">
            <table class="table table-striped align-middle mb-0">
            <thead>
                <tr>
                    <th>Тема</th>
                    <th>Кому</th>
                    <th>Прочитано</th>
                    <th>Надіслано</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Subject</td>
                        <td>@item.OtherUserName</td>
                        <td>@(item.IsRead ? "Так" : "Ні")</td>
                        <td>@item.SentAt.ToKyivString()</td>
                        <td class="text-end">
                            <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-outline-primary">Відкрити</a>
                        </td>
                    </tr>
                }
            </tbody>
            </table>
        </div>
    </div>
}
